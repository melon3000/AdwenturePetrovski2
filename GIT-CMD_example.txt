git init
git --globaal --list
git config --globaal user.name ""
git config --globaal user.email ""

---
git status
git add .
git commit -a -m "tegevus mis on tehtud"

git push

--
ssh võti loomiseks:
ida_rsa.pub on vaja lisada githubi nagu deploy key
ssh-keyegen -o -t rsa -C "email@exmaple.com"ы
